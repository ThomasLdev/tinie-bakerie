{% props label, href = null %}

{% set tag = href ? 'a' : 'button' %}

<{{ tag }}
    {% if href %}href="{{ href }}"{% else %}type="button"{% endif %}
    aria-label="{{ label }}"
    {{ attributes.defaults({class: 'icon-btn'}) }}
>
    {% block content %}{% endblock %}
</{{ tag }}>
