<nav class="bg-white">
    <div class="max-w-7xl mx-auto xl:px-0 px-4 border-b-2">
        <div class="flex justify-between items-center py-4">
            <!-- Logo -->
            <div class="flex items-center w-100 justify-center md:justify-start flex-col md:flex-row">
                <h1 class="text-2xl font-playfair font-bold text-gray-900 tracking-wide">
                    {{ 'app.global.site'|trans|upper }}
                </h1>
                <p class="text-xs text-gray-500 ml-2 mt-1">FOOD & FUN</p>
            </div>

            <!-- Social Icons -->
            <div class="flex items-center space-x-4 hidden md:block">
                <div class="flex space-x-2">
                    <div class="w-6 h-6 bg-gray-300 rounded-full"></div>
                    <div class="w-6 h-6 bg-gray-300 rounded-full"></div>
                    <div class="w-6 h-6 bg-gray-300 rounded-full"></div>
                    <div class="w-6 h-6 bg-gray-300 rounded-full"></div>
                    <div class="w-6 h-6 bg-gray-300 rounded-full"></div>
                </div>
            </div>
        </div>

        <!-- Menu -->
        <div class="border-t border-gray-200">
            <div class="flex space-x-8 py-5 text-sm font-medium text-gray-600">
                <a href="{{ path('app_post_index') }}" class="hover:text-gray-900">EXPLORE</a>
                <a href="#" class="hover:text-gray-900">CATEGORIES</a>
                <a href="#" class="hover:text-gray-900">TAGS</a>
                <a href="#" class="hover:text-gray-900">ABOUT</a>
            </div>
        </div>
    </div>
</nav>

